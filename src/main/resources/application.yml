spring:
  datasource:
    password: 13821382P.a
    url: jdbc:mysql://localhost:3306/trackerdb
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        globally_quoted_identifiers: true
    show-sql: false
    open-in-view: true
  jackson:
    serialization:
      fail-on-empty-beans: false